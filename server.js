import app from './src/app.js'

const port = process.env.PORT || 3001;

app.listen(port, ()=> {
  console.log( `Servidor iniciado na porta local: ${port}`)
})

/* URL = "https://jpnc1695nodeapi.herokuapp.com/"
 */










